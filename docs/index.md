# FastBase

This is a quick way to get the develpment environment I'm currently using. It also include some development guidelines that shold be customized for the current project. Replace this content with an appropriate index page for your project. Review files updating project name, etc.

**Run the application**:

```bash
uvicorn main:app --reload --port 8910
```
